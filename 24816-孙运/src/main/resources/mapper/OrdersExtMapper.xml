<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sy.mapper.OrderExtMapper">
<select id="findOrdersExts" parameterType="com.sy.bean.OrdersExt" resultMap="com.sy.bean.OrdersExt">
    select u.username,u.address,o.number,o.createtime,i.name,i.price,(
select SUM(i.price) from user u,orders o,items i,orderdetail od
where o.number =#{number}  and o.user_id=u.id and o.id =od.orders_id and od.items_id=i.id )sumprice from user u,orders o,items i,orderdetail od
where o.number =#{number}  and o.user_id=u.id and o.id =od.orders_id and od.items_id=i.id

</select>

</mapper>
