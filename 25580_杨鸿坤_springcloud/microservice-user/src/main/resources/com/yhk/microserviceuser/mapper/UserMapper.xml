<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yhk.microserviceuser.mapper.UserMapper">
<!--    查询所有用户-->
    <select id="findAllUser" resultType="User">
        select * from sys_user
    </select>
<!--    根据ID查询用户-->
    <select id="findUser" parameterType="java.lang.Integer" resultType="User">
        select * from sys_user where id=#{id}
    </select>
<!--添加用户-->
    <insert id="insertUser" parameterType="User">
            insert into sys_user(username,password) values (#{username},#{password})
     </insert>
<!--更新用户-->
    <update id="upUser" parameterType="User">
        update sys_user set username=#{username} ,password=#{password} where id=#{id}
    </update>
<!--    删除用户-->
    <delete id="delUser" parameterType="User">
        delete from sys_user where id=#{id}
    </delete>
</mapper>
