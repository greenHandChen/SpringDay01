server:
  port: 8082

spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_transaction?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root
  application:
    name: service

mybatis:
  typeAliasesPackage: com.hand.servicetest.Entity
  mapperLocations: classpath:mappers/*.xml
