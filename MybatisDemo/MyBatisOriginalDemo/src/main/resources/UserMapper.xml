<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="test">
    <select id="findUserById" parameterType="java.lang.Integer" resultType="com.mybatis.demo.storeDto.User">
        select  * from user where id = #{id}
    </select>

    <select id="findUserByName" parameterType="java.lang.String" resultType="com.mybatis.demo.storeDto.User">
        select  *  from user where username like ${value}
    </select>

    <insert id="inertIntoUser" parameterType="com.mybatis.demo.storeDto.User" >
        insert into user(username,birthday,sex,address) values(#{username},#{birthday},#{sex},#{address})
    </insert>

    <insert id="insertGetId" parameterType="com.mybatis.demo.storeDto.User">
        <selectKey keyProperty="id" resultType="java.lang.Integer" order="AFTER">
            select LAST_INSERT_ID()
        </selectKey>
        insert into user(username,birthday,sex,address) values(#{username},#{birthday},#{sex},#{address})
    </insert>

    <update id="updateUser" parameterType="java.lang.Integer">
        update user set username = "梁永信" where id=#{id}
    </update>

    <delete id="delUserInfo" parameterType="java.lang.Integer">
        delete from user where id=#{user_id}
    </delete>

</mapper>
