<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.mapper.UserMapper">
    <!--这里的命名空间要设置为UserMapper的全限定名，也就是要找到它的目录结构位置-->
    <select id="findUserById" parameterType="Integer" resultType="User">
        select  * from user where id = #{id}
    </select>

    <select id="findUserByName" parameterType="java.lang.String" resultType="User">
        select  *  from user where username like ${value}
    </select>

    <insert id="addUser" parameterType="User" >
        insert into user(username,birthday,sex,address) values(#{username},#{birthday},#{sex},#{address})
    </insert>

    <select id="findUserByPojo" parameterType="com.mybatis.pojo.UserQueryPojo" resultType="User">
        select * from User where address like '%${user.address}%' and sex = #{user.sex}
    </select>

    <select id="findUserByMap" parameterType="java.util.Map" resultType="User">
        select * from user where username like concat(concat('%',#{username}),'%')
    </select>

    <select id="findUserReturnMap" parameterType="int" resultMap="getUserMap" >
        select id as num,username as na,sex as sx from user where id = #{id}
    </select>

    <resultMap id="getUserMap" type="User">
        <id column="num" property="id"/>
        <result column="na" property="username"/>
        <result column="sx" property="sex"/>
    </resultMap>
    
    
    <select id="findUserByWhereAndIf" parameterType="User" resultType="User">
        select * from user
        <where>
            <if test="id !=null ">
                and id = ${id}
            </if>
            <if test="address !=null and address !=''">
                and address like '%${address}%'
            </if>
        </where>
    </select>

</mapper>
