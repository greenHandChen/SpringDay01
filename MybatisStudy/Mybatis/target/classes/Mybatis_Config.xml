<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="jdbc.properties">
    </properties>
    <!--开启缓存配置，默认全局开启所有映射器的缓存配置-->
    <settings>
        <setting name="cacheEnabled" value="true"/>
    </settings>
    <typeAliases>
        <package name="mybatis.enity"/>
        <package name="mybatis.vo"/>
        <package name="mybatis.dao.daoMapper"/>
    </typeAliases>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"></transactionManager>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driverClassName}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.passowrd}"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <!--单文件注册方式-->
        <!--<mapper resource="mybatis/dao/daoMapper/UserMapper.xml"></mapper>-->
        <!--<mapper resource="xml/UserMapper.xml"></mapper>-->
        <!--多文件注册方式-->
        <package name="mybatis.dao.daoMapper"/>
    </mappers>
</configuration>
