<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatics.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jmx.clientproducer.mapper.UserMapper">
    <!--<select id="queryUserDemo" resultType="com.springbootdemo.dto.UserDemo">
        select * from user_demo
    </select>
    <select id="getUser"  parameterType="com.springbootdemo.dto.User" resultType="com.springbootdemo.dto.User">
        select * from user where id=#{id} and username=#{username}
    </select>-->
    <select id="insertUser" parameterType="com.jmx.clientproducer.model.UserDemo">
          insert into user_demo values(#{userId},#{userName},#{userSex})
    </select>
    <select id="queryUser" resultType="com.jmx.clientproducer.model.UserDemo">
        select * from user_demo
    </select>

</mapper>