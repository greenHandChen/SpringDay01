server:
  port: 7001


#eureka
eureka:
  client:
    register-with-eureka: false  #不给自身服务注册
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
  instance:
   hostname: localhost
   prefer-ip-address: true

