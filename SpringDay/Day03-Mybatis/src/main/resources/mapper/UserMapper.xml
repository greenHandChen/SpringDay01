<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="test">
    <!--根据用户id查询用户信息-->
    <select id="findUserById" parameterType="java.lang.Integer" resultType="SpringDay.domain.User">
        SELECT * from USER where id=#{id}
    </select>

    <!--根据用户名进行模糊查询-->
    <select id="findUserByUsername" parameterType="java.lang.String" resultType="SpringDay.domain.User">
        select * from user where username like ${value}
    </select>

    <!--添加用户-->
    <insert id="addUser" parameterType="SpringDay.domain.User">
        INSERT into user(username,birthday,sex,address) values(#{username},#{birthday},#{sex},#{address})
    </insert>

    <!--添加用户返回添加的主键-->
    <insert id="addUserGetId" parameterType="SpringDay.domain.User">
        <selectKey keyProperty="id" resultType="java.lang.Integer" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO USER(username,birthday,sex,address) values(#{username},#{birthday},#{sex},#{address})
    </insert>
    
    <!--添加用户返回UUID-->
    <insert id="addUserGetUUID" parameterType="SpringDay.domain.User">
        <selectKey keyProperty="id" resultType="java.lang.Integer" order="BEFORE">
            SELECT UUID()
        </selectKey>
        INSERT INTO USER(id,username,birthday,sex,address) values(#{id},#{username},#{birthday},#{sex},#{address})
    </insert>

</mapper>