<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/context
                           http://www.springframework.org/schema/context/spring-context.xsd">
    <!--包扫描配置-->
    <context:component-scan base-package="Transaction.*"/>
    <!--加载外部配置文件-->
    <context:property-placeholder location="classpath:dataSource.properties"></context:property-placeholder>

    <bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource" destroy-method="close">
        <property name="driverClassName" value="${jdbc.driverClassname}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.uername}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>

    <bean id="IAccountDaoImpl" class="Transaction.Dao.impl.IAccountDaoImpl">
        <property name="dataSource" ref="dataSource"></property>
    </bean>
    <!--配置事务管理器-->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>
    
    <bean id="IAcountServiceImpl" class="Transaction.Service.impl.IAcountServiceImpl">
        <property name="iAccountDao" ref="IAccountDaoImpl"/>
    </bean>

    <!--配置事务代理工厂-->
    <bean id="transactionProxyFactoryBean" class="org.springframework.transaction.interceptor.TransactionProxyFactoryBean">
       <!--配置事务管理器-->
        <property name="transactionManager" ref="transactionManager"/>
        <!--配置目标类-->
        <property name="target" ref="IAcountServiceImpl"/>
        <!--配置目标类接口-->
        <property name="proxyInterfaces" value="Transaction.Service.IAcountService"/>
        <!--
            配置哪些方法需要事务控制以及如何控制
            prop配置格式：PROPAGATION,ISOATION,REDONLY,-EXCEPTION,+EXCEPTION
            
        -->
        <property name="transactionAttributes">
            <props>
                <prop key="transfer">PROPAGATION_REQUIRED,ISOLATION_DEFAULT</prop>
            </props>
        </property>
    </bean>
</beans>