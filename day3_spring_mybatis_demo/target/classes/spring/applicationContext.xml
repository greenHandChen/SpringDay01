<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="/"/>
    <context:property-placeholder location="config/db.properties"/>
    <!--创建数据源 -->
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource">
        <property name="driverClassName" value="${db.driverClassName}"/>
        <property name="jdbcUrl" value="${db.url}"/>
        <property name="username" value="${db.username}"/>
        <property name="password" value="${db.password}"/>
    </bean>

    <!-- SqlSessionFactory (全局) -->
    <bean id="sessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="configLocation" value="mybatis/SqlMapConfig.xml"/>
    </bean>

    <!-- 1.SqlSessionDaoSupport代理类 -->
    <!-- 配UserDao,注入SqlSessionFactory -->
    <bean id="userDao" class="com.dao.UserDaoImpl">
        <property name="sqlSessionFactory" ref="sessionFactory"/>
    </bean>

    <!-- 2.mapper代理开发方式 -->
    <!-- 2.1单个配置mapper代理类-->
    <bean id="userMapper" class="org.mybatis.spring.mapper.MapperFactoryBean">
        <!-- 配置代理类接口 -->
        <property name="mapperInterface" value="com.mapper.UserMapper"/>
        <!-- 注入SqlSessionFactory -->
        <property name="sqlSessionFactory" ref="sessionFactory"/>
    </bean>

    <!-- 2.2批量扫描mapper代理类,生成的代理类的bean的id默认是按首字母小写 -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <!-- 配置mapper扫描接口的路径 -->
        <property name="basePackage" value="com.mapper"/>
        <!-- 注入sqlSessionFactory(如果至配置了一个SqlSessionFactory,可以不用指定注入sqlSessionFactory) -->
        <property name="sqlSessionFactoryBeanName" value="sessionFactory"/>
    </bean>
</beans>