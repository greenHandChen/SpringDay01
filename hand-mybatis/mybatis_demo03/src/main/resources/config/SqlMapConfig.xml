<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!--    引入外部数据库配置文件-->
<!--    <properties resource="config/db.properties"/>-->
    <!--    配置mybatis的环境信息，比如数据源以及事务控制，整合spring后就交给spring控制-->

    <typeAliases>
    <!--  单个别名定义      -->
    <!--   <typeAliases type ="com.lhb.domain.User " alias="user"    -->
<!--        批量别名定义，别名取得是类名，不区分大小写-->
        <package name="com.lhb"/>
    </typeAliases>
    


    <environments default="development">
        <environment id="development">
    <!--        配置JDBC事务控制，由mybatis进行管理-->
            <transactionManager type="JDBC"></transactionManager>
    <!--        配置数据源，采用mybatis连接池-->
            <dataSource type="POOLED">
<!--                <property name="driver" value="${db.driverClassName}"/>-->
<!--                <property name="url" value="${db.url}"/>-->
<!--                <property name="username" value="${db.username}"/>-->
<!--                <property name="password" value="${db.password}"/>-->
                <property name="driver" value="com.mysql.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/hand_mybatis?useUnicode=true&amp;characterEncoding=utf-8&amp;useSSL=false"/>
                <property name="username" value="root"/>
                <property name="password" value="123456"/>
            </dataSource>
        </environment>
    </environments>

<!--    加载映射配置文件-->
    <mappers>
        <package name="com.lhb.mapper"/>
    </mappers>



</configuration>