<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.txz.mapper.OrderMapper">

    <cache/>

    <resultMap id="ordersAndUserResMap" type="OrderExt">
        <id column="id" property="id"/>
        <result column="user_id" property="user_id"/>
        <result column="number" property="number"/>
        <result column="note" property="note"/>
        <association property="user" javaType="User">
            <id column="user_id" property="id"/>
            <result column="username" property="username"/>
            <result column="address" property="address"/>
        </association>
    </resultMap>

    <resultMap id="ordersAndOrderDetailRstMap" type="OrderExt" extends="ordersAndUserResMap">
        <collection property="orderDetails" ofType="OrderDetail" >
            <id column="detailId" property="id"/>
            <result column="items_id" property="items_id"/>
            <result column="items_num" property="items_num"/>
        </collection>
    </resultMap>

        <!--根据订单ID查询订单以及用户信息-->
    <select id="findOrdersExt" parameterType="OrderExt" resultType="OrderExt">
        select o.id,o.user_id,o.number,o.createtime,o.note,u.username,u.address from
        orders o,user u where o.user_id=u.id
        <if test="id != null">
            and o.id=#{id}
        </if>
    </select>

    <!--根据订单ID查询订单以及用户信息map-->
    <select id="findOrdersByResultMap" parameterType="OrderExt" resultMap="ordersAndUserResMap">
        select o.id,o.user_id,o.number,o.createtime,o.note,u.username,u.address from
        orders o,user u where o.user_id=u.id
        <if test="id != null">
            and o.id=#{id}
        </if>
    </select>

    <!--根据订单ID查询订单，订单的明细以及用户信息 -->
    <select id="findOrdersAndOrderDetail" parameterType="OrderExt" resultMap="ordersAndOrderDetailRstMap">
        select o.id,o.user_id,o.createtime,o.note,u.username,u.address,od.items_id detailId,od.items_num
        from orders o,user u,orderdetail od where o.user_id=u.id and o.id=od.orders_id
        <if test="id != null">
            and o.id=#{id}
        </if>
    </select>

    <!--延迟加载 -->
    <select id="findOrderInfoByLazyLoad" parameterType="integer" resultMap="lazyLoadRstMap">
        select * from orders where id=#{id}
    </select>

    <resultMap id="lazyLoadRstMap" type="OrderExt">
        <!--订单配置信息映射 -->
        <id column="id" property="id"/>
        <result column="user_id" property="user_id"/>
        <result column="number" property="number"/>
        <result column="note" property="note"/>
        <!--一对一映射 -->
        <association property="user" select="com.txz.mapper.OrderMapper.findUserById" column="user_id"/>
    </resultMap>

    <!--根据ID查询用户信息 -->
    <select id="findUserById" parameterType="integer" resultType="User">
        select * from user where id=#{user_id}
    </select>

    <!--添加用户 -->
    <select id="addUser" parameterType="User" resultType="integer">
        insert into user values(#{id},#{username},#{birthday},#{sex},#{address})
    </select>

</mapper>