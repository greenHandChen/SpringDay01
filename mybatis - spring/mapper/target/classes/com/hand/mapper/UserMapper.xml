<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hand.mapper.UserMapper">
    <select id="findUserByUser" parameterType="userQueryVO" resultType="user">
        SELECT * FROM USER u WHERE u.address =#{user.address} and u.sex=#{user.sex}
    </select>
    <select id="showUsers" resultType="com.hand.pojo.User">
        select  * FROM USER
    </select>
    <select id="findUserById" parameterType="java.lang.Integer" resultType="com.hand.pojo.User">
        SELECT * FROM  USER WHERE id =#{id}
    </select>
    <select id="findUserByName" parameterType="java.lang.String" resultType="com.hand.pojo.User">
        SELECT * FROM  USER WHERE username like #{username}
    </select>
    <insert id="addUser" parameterType="com.hand.pojo.User">
        <selectKey keyProperty="id" resultType="java.lang.Integer" order="AFTER">
           SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT  INTO USER VALUES (#{id},#{username},#{birthday},#{sex},#{address})
    </insert>
    <insert id="addUserByUUID" parameterType="com.hand.pojo.User">
        <selectKey keyProperty="address" resultType="java.lang.String" order="BEFORE">
            SELECT UUID()
        </selectKey>
        INSERT  INTO USER VALUES (#{id},#{username},#{birthday},#{sex},#{address})
    </insert>

</mapper>