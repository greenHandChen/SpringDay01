<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "- //mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hand.mapper.OrderMapper">
    <select id="findOrdersExt" parameterType="com.hand.pojo.OrdersExt" resultType="com.hand.pojo.OrdersExt">
        select o.id,
        o.user_id,
        o.number,
        o.note,
        u.username,u.address
        from orders o,user u
        where o.user_id=u.id
        <if test="id != null">
            and o.id = #{id}
        </if>
    </select>



    <resultMap id="ordersAndUserRstMap" type="com.hand.pojo.OrdersExt1">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="number" property="mumber"/>
        <result column="note" property="note"/>
        <association property="user" javaType="com.hand.pojo.User">
            <id column="user_id" property="id"/>
            <result column="username" property="username"/>
            <result column="address" property="address"/>
        </association>
    </resultMap>

    <select id="findOrdersByResultMap" parameterType="com.hand.pojo.OrdersExt1" resultMap="ordersAndUserRstMap">
        select o.id,o.user_id,o.number,o.note,u.username,u.address
        from orders o,user u
        where o.user_id=u.id
        <if test="id != null">
            and o.id = #{id}
        </if>
    </select>

    <select id="findOrdersAndOrderDetail" parameterType="com.hand.pojo.OrdersExt2" resultMap="ordersAndOrderDetailRstMap">
        select o.id,
        o.user_id,
        o.number,
        o.createtime,
        o.note,
        u.username,
        u.address,
        od.id,
        od.orders_id,
        od.items_id,
        od.items_num
        from orders o,user u,orderdetail od
        where o.user_id = u.id
        and o.id= od.orders_id
        <if test="id!=null">
            and o.id=#{id}
        </if>
    </select>

    <resultMap id="ordersAndOrderDetailRstMap" type="com.hand.pojo.OrdersExt2" extends="ordersAndUserRstMap">
        <collection property="orderDetail" ofType="com.hand.pojo.OrderDetail">
            <id column="id" property="id"/>
            <result column="items_id" property="itemsId"/>
            <result column="items_num" property="itemsNum"/>
        </collection>
    </resultMap>
    
    <select id="findOrderInfoByLazyLoad" parameterType="java.lang.Integer" resultMap="lazyLoadRstMap">
        select * from orders where id=#{id}
    </select>

    <resultMap id="lazyLoadRstMap" type="com.hand.pojo.OrdersExt2">
        <id column="id" property="id"></id>
        <result column="user_id" property="userId"></result>
        <result column="number" property="mumber"></result>
        <result column="note" property="note"></result>
        <association property="user" select="com.hand.mapper.UserMapper.findUserById" column="user_id"></association>
    </resultMap>

</mapper>