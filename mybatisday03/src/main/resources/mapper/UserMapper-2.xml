<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.UserMapper">
    <!--使用了别名包-->
    <select id="findUserById" resultType="User">
        select * from user where id=#{id}
    </select>
    <select id="findUserByUsername" resultType="User">
        select * from user where username=${value}
    </select>
    <insert id="addUser">
        insert into user(username,birthday,sex,address) values(#{username},#{birthday},#{sex},#{address})
    </insert>
    <select id="findUserByUser" parameterType="UserQueryVO" resultType="User">
        select * from user u where u.address like  '%${user.address}%' and u.sex=#{user.sex}
    </select>
    <select id="findUserByMap" resultType="User">
        select * from user u where u.username like concat(concat('%',#{username}),'%')
    </select>
    <select id="countUser" resultType="java.lang.Integer">
        select count(*) from user;
    </select>
    <select id="findUsernameAndAddressById" resultType="User">
        select u.username,u.address from user u where u.id=#{id}
    </select>
    <resultMap id="userResultMap" type="User">
        <id column="number" property="id"/>
        <result column="name" property="username"/>
        <result column="location" property="address"/>
    </resultMap>
    <select id="testResultMap" resultMap="userResultMap">
        select id as number,username as name,address as location from user where id=#{id}
    </select>
    <select id="testIfAndWhereLabel" resultType="User">
        select * from user
        <where>
            <if test="id!=null">
                and id=#{id}
            </if>
            <if test="username!=null">
                and username like concat(concat('%',#{username}),'%')
            </if>
        </where>
    </select>
</mapper>