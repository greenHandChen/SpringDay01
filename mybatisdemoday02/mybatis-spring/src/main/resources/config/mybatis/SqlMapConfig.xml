<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!-- 自定义别名 -->
    <typeAliases>
        <!-- 单个别名定义 -->
        <!-- <typeAlias type="com.itheima.mybatis.po.User" alias="user"/> -->

        <!-- 批量别名定义（推荐） -->
        <!-- package：指定包名称来为该包下的po类声明别名，默认的别名就是类名（首字母大小写都可） -->
        <package name="com.ljx.mybatisspring.po" />
    </typeAliases>

    <!-- 加载映射文件 -->
    <mappers>
        <mapper resource="config/mybatis/sqlmap/User.xml"/>
        <!-- 批量加载映射文件 ，mapper的xml与接口同一目录下-->
        <package name="com.ljx.mybatisspring.mapper" />

    </mappers>
</configuration>
