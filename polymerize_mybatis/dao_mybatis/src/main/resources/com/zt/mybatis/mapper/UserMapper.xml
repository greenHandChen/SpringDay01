<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zt.mybatis.mapper.UserMapper">
    <select id="findUserById" parameterType="java.lang.Integer" resultType="com.zt.mybatis.domain.User">
        SELECT * FROM user WHERE id = #{id}
    </select>
    
    <select id="findUserByUsername" parameterType="java.lang.String" resultType="com.zt.mybatis.domain.User">
        SELECT * FROM USER WHERE username LIKE ${value}
    </select>

    <insert id="addUser" parameterType="com.zt.mybatis.domain.User">
        INSERT INTO user (username, birthday, sex, address) VALUES (#{username}, #{birthday}, #{sex}, #{address})
    </insert>

    <select id="findUserByUser" parameterType="UserQueryVO" resultType="User">
        SELECT * FROM user u WHERE u.address LIKE '%${user.address}%' AND u.sex = #{user.sex}
    </select>

    <resultMap id="userResultMap" type="User">
        <id column="num" property="id"/>
        <result column="name" property="username"/>
        <result column="address" property="address"/>
    </resultMap>
    <select id="findUserByMap" parameterType="java.util.Map" resultMap="userResultMap">
        SELECT id num, username name, address FROM user u WHERE u.username LIKE CONCAT(CONCAT('%', #{username}), '%')
    </select>
    
    <select id="findUserByIfAndWhereLabel" parameterType="User" resultType="User">
        SELECT * FROM user
        <where>
            <if test="id != null">
                AND id = #{id}
            </if>
            <if test='username != null and username != " "'>
                AND username LIKE CONCAT(CONCAT('%', #{username}), '%')
            </if>
        </where>
    </select>

</mapper>