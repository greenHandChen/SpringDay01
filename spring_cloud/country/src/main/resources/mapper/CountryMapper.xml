<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hand.country.mapper.CountryMapper">

    <resultMap id="countryMap" type="com.hand.country.entity.Country">
        <id column="country_id" property="countryId"/>
        <result column="country_name" property="countryName"/>
    </resultMap>
    <select id="findAll" resultMap="countryMap">
        select * from country
    </select>


    <select id="findCountryById" parameterType="java.lang.Integer" resultMap="countryMap">
        select * from country where country_id = #{id}
    </select>
</mapper>