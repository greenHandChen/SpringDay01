<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="huanglehao.study.ms.mapper.UserMapper">

    <select id="findUserById" parameterType="int" resultType="huanglehao.study.ms.po.User" flushCache="true">
        SELECT * FROM USER WHERE id = #{id}
    </select>

</mapper>
