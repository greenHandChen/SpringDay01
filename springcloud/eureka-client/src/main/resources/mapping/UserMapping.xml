<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springcloud.eurekaclient.mapper.UserMapper">
    <select id="getUserById" parameterType="java.util.Map" resultType="com.springcloud.eurekaclient.bean.User">
        select * from user where id = #{map.id}
    </select>
    <delete id="deleteUserById" parameterType="java.util.Map">
        delete from user where id = #{map.id}
    </delete>
    <update id="updateUser" parameterType="java.util.Map">
        update user
        <set>
            <if test="map.user.username != null">
                username = #{map.user.username},
            </if>
            <if test="map.user.birthday != null">
                birthday = #{map.user.birthday},
            </if>
            <if test="map.user.address != null">
                address = #{map.user.address},
            </if>
            <if test="map.user.sex != null">
                sex = #{map.user.sex},
            </if>
        </set>
        where id = #{map.user.id}
    </update>
    <!--    自动生成主键-->
    <insert id="addUser" parameterType="java.util.Map" useGeneratedKeys="true" keyProperty="map.user.id" keyColumn="id">
        insert into user(username,birthday,sex,address)
        values(#{map.user.username},#{map.user.birthday},#{map.user.sex},#{map.user.address})
    </insert>

</mapper>